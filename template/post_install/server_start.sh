#!/bin/bash

# Available ENV vars:
# $SERVER_DIR:      The installation directory of the server and this script
# $POST_INSTALL:    The directory where some post installation files are mounted (default conf, ...)
# $INITIAL_INSTALL: Is set to "true" when this is the initial installation.

# START SERVER HERE

echo "server_start"
# Block for at least the server runtime before exit.
exit 0
